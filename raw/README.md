# raw/

Drop source documents here: articles, papers, notes, transcripts, data files. Markdown preferred. Images go in `assets/`.

Files here are immutable — the LLM reads them, never edits them.

Tip: the Obsidian Web Clipper browser extension converts web articles to markdown for this folder.
